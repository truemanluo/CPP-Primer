#include <iostream>
#include <vector>
#include <string>

using std::vector;
using std::string;
using std::cin;
using std::cout;
using std::endl;

int get_size()
{
    return -1;
}
int main()
{
    for (int sz = get_size(); sz <= 0; sz = get_size())
        // cout << sz << endl; 
        ;
    // cout << sz << endl;

    return 0;
}